import { AccountPageHeader } from "../../components/Custom/AccountStyles";

export const AccountHeader = () => {

    return (
        <AccountPageHeader>
            <h1>My Account</h1>
        </AccountPageHeader>
    );

}