import { AccountPageHeader } from "../../components/AccountStyles";

export const AccountHeader = () => {

    return (
        <AccountPageHeader>
            <h1>My Account</h1>
        </AccountPageHeader>
    );

}